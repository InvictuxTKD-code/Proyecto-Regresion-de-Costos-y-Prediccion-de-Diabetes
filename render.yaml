services:
  - type: web
    name: prediccion-diabetes-seguro
    env: python
    buildCommand: "poetry install"
    startCommand: "poetry run streamlit run app.py --server.port $PORT --server.address 0.0.0.0"
    pythonVersion: 3.11.9
    autoDeploy: true
